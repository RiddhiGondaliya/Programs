  
    static int calculate_Area(Rectangle arr[]){
        
       int max  = 0;
       for(int i =0; i< arr.length;i++){
           max = Math.max(max,arr[i].area());
           // max = Math.max(max, arr[i].length *arr[i].breadth);
           
          
       }
         return max;
